# TBPprojekt
Web aplikacija za geografsku evidenciju posjeta nacionalnih parkova

Za projekt je potrebno imati instalirano sljedeće:

1. python3 ( sudo apt install python)
2. postGis (sudo apt install postgis postgresql-postgis)
3. streamlit ( pip install streamlit )
4. psycopg2 ( pip install pip install psycopg2-binary)

Za pokretanje terminal treba pozicionirati u skinuti folder ( /TBPprojekt ) i upisati naredba streamlit run ./map.py
